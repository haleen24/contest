#include <iostream>

int main() {

	int n, s, tmp;

	_int64 sum = 0;

	std::cin >> n >> s;

	for (int i = 0; i < n; i++) {

		std::cin >> tmp;

		sum += tmp;

	}

	sum -= s;

	std::cout << int(sum / ( n - 1) );

}
